

                // ACCESSING VARIABLE
function Hello() {
     let count = 5;
      return (
         <>
        <h1>{count}</h1>
          
           <h2>{count * count}</h2>
           </> );
           }
           export default Hello;